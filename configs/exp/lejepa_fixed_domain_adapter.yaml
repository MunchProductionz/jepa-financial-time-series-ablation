defaults:
  - lejepa_fixed_predictive_sigreg_adapter_whitened
  - _self_

run_name: tft_lejepa_fixed_predictive_sigreg_domain_adapter_last1_h1

jepa:
  lejepa:
    representation:
      mode: adapter_whitened
      domain_context:
        enabled: true
        source: static
        input_dim: null

logging:
  wandb:
    tags: ["lejepa", "fixed", "predictive_sigreg", "adapter_whitened", "domain_context"]
