defaults:
  - base
  - _self_

run_name: tft_no_jepa_1d

model:
  target: ablation_study_jepa.models.tft:TFT
  hidden_dim: 128
  num_transformer_blocks: 4
  num_attention_heads: 4
  dropout: 0.1

jepa:
  enabled: false
  num_jepa_layers: 0
  layer_selection_mode: none # values: last_L, last_k, manual, all, none

logging:
  wandb:
    tags: ["baseline", "no_jepa", "tft"]
