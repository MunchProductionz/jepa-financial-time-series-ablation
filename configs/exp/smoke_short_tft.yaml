defaults:
  - smoke_short_base
  - _self_

run_name: smoke_short_tft

model:
  target: ablation_study_jepa.models.tft:TFT

jepa:
  enabled: false
  num_jepa_layers: 0
  layer_selection_mode: none

logging:
  wandb:
    tags: ["smoke", "short", "baseline", "no_jepa"]
